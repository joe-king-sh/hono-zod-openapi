{
	"name": "shared",
	"version": "1.0.0",
	"description": "Shared schemas and types for TODO API",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"lint": "biome lint src",
		"lint:fix": "biome lint --write src",
		"format": "biome format --write src",
		"check": "biome check src",
		"check:fix": "biome check --write src"
	},
	"dependencies": {
		"@hono/zod-openapi": "^0.18.2",
		"zod": "^3.24.1"
	},
	"devDependencies": {
		"typescript": "^5.7.0"
	}
}
